//! Text tokenization (placeholder for a future sprint).

#![allow(dead_code)]

/// Placeholder tokenizer.
#[derive(Debug, Default)]
pub struct Tokenizer;

impl Tokenizer {
    pub fn new() -> Self {
        Self
    }
}
